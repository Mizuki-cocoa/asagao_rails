<% @page_title = "会員の詳細" %>

<h1><%= @page_title %></h1>

<div class="toolbar">
    <%= link_to "編集", [:edit, @member] %>
    <%= link_to "投票した記事一覧", voted_entry_path(@member.id) %>
</div>

<%= render "body" %>
